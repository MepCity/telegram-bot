[phases.setup]
aptPkgs = ['libreoffice-writer', 'libreoffice-calc', 'tesseract-ocr', 'tesseract-ocr-tur', 'fonts-liberation']

[phases.install]
cmds = ['pip install -r requirements.txt']

[start]
cmd = 'python3 bot.py'
